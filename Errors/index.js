const BadRequest = require('./BadRequest')
const CustomError = require('./CustomError')
const NotFound = require('./NotFound')
const Unauthenticated = require('./Unauthenticated')

module.exports = {
    BadRequest, CustomError, NotFound, Unauthenticated
}